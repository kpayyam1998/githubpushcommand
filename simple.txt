```bash
    conda create -p (environment_name) python
```

```bash
    conda activate environment_name
```

```bash
   git init
   git add . 
   git branch (Which branch we are using)
   git checkout -b main (Switch to main branch)

   (git branch creation)

   git branch -c staging(branch_name)

   switching to staging branch
   git checkout staging

    git status - we can see the new changes in the files

    git add . ( all files)

    git commit -m "change name"

    git push origin head (this command will push changes to the staging branch) 

    go to github raise pull request
    f
```
